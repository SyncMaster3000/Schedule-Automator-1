- [Schedule app](schedule-app.md) — build/verify via `PORT=4999 BASE_PATH=/ pnpm --filter @workspace/schedule run build` (no vue-tsc); restart api-server after backend edits (dev rebuilds dist bundle); /call dispatcher needs READONLY allowlist for read channels; data model excluded/roundtable_hours/is_section + array teacher_ids; assessment-import utp_number="" uniqueness limit; is_outside_period flag for overflow shift.
- [Word export templates](docx-export-templates.md) — template path must be process.cwd()+"/templates" (not __dirname-relative); templates at artifacts/api-server/templates/; two tables per doc (schedule + signer); row 2 is style template for 7-col, row 1 for 8-col.
